#include<stdio.h>

int main(int argc, char const *argv[])
{   
    int a;
    printf("\n: number of character in printf : %d -->\n", printf("Enter the number: "));
    printf("\n%d",scanf("%d", &a)); // its all about specifier if format specifier getting value is %d then its true either false, like that all to others  
    return 0;
}
